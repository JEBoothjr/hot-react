<!doctype html>
<html>
<head>
    <meta charset="utf-8" />
    {{#if csrfToken}}
    <meta name="csrf_token" content="{{csrfToken}}" />
    {{/if}}
    <title>{{title}}</title>
    {{{block.css}}}
</head>
<body>

    {{{body}}}

    {{{block.js}}}
</body>
</html>
